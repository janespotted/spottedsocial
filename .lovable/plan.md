

## Enable Auto-Confirm Email Signups

### Change
Use the `cloud--configure_auth` tool to enable auto-confirm for email signups. This removes the email verification step — users can sign in immediately after creating an account.

### Steps
1. Configure auth to enable auto-confirm email signups
2. No code changes needed — the Auth page already handles both flows (with and without email confirmation)

