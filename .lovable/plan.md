

# Change "Find Friends" → "Who's Out"

## Change (1 file)

### `src/components/FriendSearchModal.tsx` — line 196

Change the DrawerTitle text from `Find Friends` to `Who's Out`:

```tsx
<DrawerTitle className="text-foreground text-center text-base">Who's Out</DrawerTitle>
```

That's it — single line change.

