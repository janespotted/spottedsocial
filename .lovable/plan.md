

# Change "Post" Button Text to "View"

Single-line change in `src/components/messages/YapTab.tsx` at line 189.

**Current:**
```tsx
<span className="bg-[#d4ff00] text-[#1a0f2e] font-bold text-xs px-3 py-1 rounded-full">Post</span>
```

**New:**
```tsx
<span className="bg-[#d4ff00] text-[#1a0f2e] font-bold text-xs px-3 py-1 rounded-full">View</span>
```

The button's action (opening the venue's Yap thread) stays exactly the same.

